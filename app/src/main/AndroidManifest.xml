<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/flavorize"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/flavorize_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flavorize" >


        <!-- Main Activity -->
        <activity
            android:name=".ui.activities.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Flavorize.MySplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



        <activity
            android:name=".ui.activities.auth.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.bookmark.BookmarkedRecipesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.editrecipe.EditRecipeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.recipedetail.RecipeDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.profile.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.draft.DraftedRecipesActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.createform.CreateRecipeFormActivity"
            android:exported="false" />
    </application>

</manifest>